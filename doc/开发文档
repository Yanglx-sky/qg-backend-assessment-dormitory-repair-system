
                   DormRepairSystem  1.0版本：

前端：无框架，使用HTML、CSS、JavaScript实现
后端：使用springboot，mp实现，三层架构
数据库：MySQL
功能模块：1.用户模块：用户登录、注册、账号3开头将识别为学生，账号2开头的将识别为维修人员
                    学生可以绑定和更改宿舍，提交和取消报修订单（文字描述）
                    维修人员可以接取订单，查看订单详情，更新订单状态
                    管理员可以查看所有订单，所有学生，所有维修人员，所有宿舍并进行管理
         2.订单模块：订单状态包括：待处理、维修中、已完成、已取消
                    订单详情只有文字描述，没有图片描述
         3.宿舍模块：每个学生只能绑定一个宿舍，每个宿舍只能由学生绑定
         4.角色模块：给予学生、维修人员、管理员不同的roleid以实现不同的权限


                    DormRepairSystem  2.0版本：

前端：使用Vue3、ElementPlus实现
后端：使用springboot，mp实现，增加阿里云oss配置用于文件上传，
     swagger配置采用springdoc+knife4j实现接口文档测试，统一异常拦截
     jwt+拦截器实现用户认证和授权，双token机制实现token的刷新和验证
     slf4j+logback实现日志记录
数据库：MySQL
新增功能：1.订单模块：订单详情包括图片上传（至多三张）
                    所有角色可查看订单的图片
         2.管理员模块：管理员可以按状态筛选订单，管理员页面实现分页查询功能
认证流程:
         1. 用户输入账号密码登录
         2. 系统验证用户信息
         3. 生成 JWT 令牌（accessToken 和 refreshToken）
         4. 前端存储令牌
         5. 每次请求携带 accessToken
         6. 当 accessToken 过期时，使用 refreshToken 刷新
         7. 当 refreshToken 也过期时，跳转到登录页